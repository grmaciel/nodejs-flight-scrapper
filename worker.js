const telegramHandler = require('./telegram_handler.js');
// const Scrapper = require('./scrapper');
// const DB = require('./db.js');

// (async () => {
//     const telegramHandler = new TelegramHandler();
//     telegramHandler.start();

    // TODO kepp everything below for testing

    // const db = new DB();
    // await db.start();

    // const scrapper = new Scrapper({departure: "Berlin"});
    // let listCheapestDeparture = await scrapper.getCheapestDeparture()

    // console.log(listCheapestDeparture);
    // for (var index in listCheapestDeparture) {
    //     console.log(listCheapestDeparture[index]);
    // }

// })();
